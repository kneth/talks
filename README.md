# My talks

- `temporal` - a brief introduction to JavaScript's `Temporal`

## How to convert to PDF

To convert from markdown to PDF:

```shell
pandoc -t beamer --syntax-highlighting=tango temporal.md -o temporal.pdf
```
